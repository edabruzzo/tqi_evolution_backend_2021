spring:
  application:
    name: servico_emprestimo
    profiles:
      active: default
    cloud:
      config:
        uri: http://localhost:8888
  config:
    import: "optional:configserver:http://localhost:8888"